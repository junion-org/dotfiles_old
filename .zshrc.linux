# Linux 専用のzsh設定

# alias
alias ls='ls -F --color=auto'
alias lla='ls -la'
alias ll='ls -l'
alias la='ls -a'
alias vi='vim'

# PATH
PATH=$HOME/bin:$PATH
PATH=/usr/local/bin:$PATH
PATH=/usr/local/mysql/bin:$PATH
export PATH

# LD_LIBRARY_PATH
# TODO:write here if you need

